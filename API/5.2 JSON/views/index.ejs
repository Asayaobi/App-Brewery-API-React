<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">
    <!-- If no recipe yet, show: -->
    <h2>
  <% if (locals.taco) { %>
        <%= locals.taco.name %>
     <% } else { %>
        Pick your favourite taco ingredient 👆
  <% } %>
    </h2>
    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->

    <h3>Ingredients:</h3>
    <ul id="ingredientsList">
      <% if (locals.taco) { %>
        <!-- show items here -->
        <li><%= locals.taco.ingredients.protein.name %>, <%= locals.taco.ingredients.protein.preparation %></li>
        <li><%= locals.taco.ingredients.salsa.name %></li>

        <!-- loop the toppings -->
        <%let toppings = locals.taco.ingredients.toppings%>
         <%for (let i = 0; i < toppings.length; i ++){%>
          <li><%= toppings[i].quantity %> of <%= toppings[i].name %></li>
          <% }%>
        <!-- alternative method
        <% toppings.forEach(topping => { %>
          <li><%= topping.quantity %> of <%= topping.name %></li> 
        <% }) %> -->
        <% } else { %>
      <li>Protein name, protein preparation</li>
      <li>Salsa name</li>
      <li>Every other topping</li>
      <% } %>
    </ul>



  </div>
</body>

</html>